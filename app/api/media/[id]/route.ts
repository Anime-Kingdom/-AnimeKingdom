import {one,env} from '@/lib/server';
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;if(!/^[0-9a-f-]{36}$/.test(id))return new Response('Not found',{status:404});try{const row=await one('SELECT mime FROM uploads WHERE id=?',id),file=await env.BUCKET?.get(id);if(!row||!file)return new Response('Not found',{status:404});return new Response(file.body as any,{headers:{'Content-Type':row.mime,'X-Content-Type-Options':'nosniff','Cache-Control':'public,max-age=31536000,immutable'}})}catch{return new Response('Image unavailable',{status:503})}}
